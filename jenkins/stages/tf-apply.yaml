name: TF Apply
agent: laptop
steps:
  - "bash -lc 'if [ -f tfplan ]; then terraform apply -no-color -input=false tfplan; else terraform apply -no-color -auto-approve -input=false; fi'"
  - "bash -lc 'terraform output -no-color'"